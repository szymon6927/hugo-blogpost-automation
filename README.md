# hugo-blogpost-automation
Blog posts publication automation from a developer for developers

## Setup

1. Install dependencies:

```bash
$ poetry install
```

2. Setup pre-commit hooks before committing:

```bash
$ poetry run pre-commit install
```
